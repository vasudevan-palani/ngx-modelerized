import { ModelRefDirective } from './model-ref.directive';

describe('ModelRefDirective', () => {
  it('should create an instance', () => {
    const directive = new ModelRefDirective();
    expect(directive).toBeTruthy();
  });
});
